# DevNet Module - Common Environment Variables
# These override the localnet defaults to connect to splice-node

# Auth mode for devnet (OAuth2 via Keycloak)
AUTH_MODE=OAuth2

# splice-node connection (accessible via host.docker.internal from containers)
LEDGER_HOST=host.docker.internal
LEDGER_PORT=8080
VALIDATOR_HOST=host.docker.internal
VALIDATOR_PORT=5012

# DevNet module directories
DEVNET_DIR=${MODULES_DIR}/devnet
DEVNET_ENV_DIR=${DEVNET_DIR}/env

# Keycloak database (separate from splice-node)
KEYCLOAK_DB_USER=keycloak
KEYCLOAK_DB_PASSWORD=keycloak
KEYCLOAK_DB_NAME=keycloakdb
KEYCLOAK_DB_PORT=5433

# PQS database (separate from splice-node)
PQS_DB_USER=cnadmin
PQS_DB_PASSWORD=supersafe
PQS_DB_NAME=pqs-app-provider
PQS_DB_PORT=5434

# Keycloak admin
KEYCLOAK_ADMIN=admin
KEYCLOAK_ADMIN_PASSWORD=admin

# Keycloak URLs (for frontend and backend)
AUTH_URL=http://keycloak.localhost:8082
KEYCLOAK_REALM=AppProvider

# Backend service port
BACKEND_PORT=8089
